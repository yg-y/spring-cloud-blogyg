spring:
  application:
    name: blogyg-server-feign
  zipkin:
    base-url: http://blogyg:8777

server:
  port: 8555

# 开启feign hystrix 熔断
feign:
  hystrix:
    enabled: true

# 注册中心
eureka:
  client:
    serviceUrl:
      defaultZone: http://blogyg:8111/eureka/


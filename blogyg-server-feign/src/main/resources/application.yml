spring:
  application:
    name: blogyg-server-feign

server:
  port: 8555

# 开启feign hystrix 熔断
feign:
  hystrix:
    enabled: true

# 注册中心
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8111/eureka/
